name: hint
template_variant: hint
reward_function: compute_score_hint
multi_turn: true
max_turns: 5
mask_response_tokens: true  # Mask <response>...</response> tokens during SFT
reward_kwargs:
  hint_penalty: 0.1
  format_score: 0.1
