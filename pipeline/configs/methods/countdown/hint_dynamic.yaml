name: hint_dynamic
template_variant: hint
reward_function: compute_score_hint_dynamic
multi_turn: true
max_turns: 6
mask_response_tokens: true
reward_kwargs:
  correct_end: 0.55
  incorrect_end: 0.45
  format_score: 0.1

# Retired 2026-08-25. Loadable so existing results stay reproducible;
# not for new work. See docs/all_methods.md.
deprecated: true
deprecated_note: "exploratory hint-scheduling ablation; never produced model weights"
